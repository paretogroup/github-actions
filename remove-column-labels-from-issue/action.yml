name: 'Remove column labels from issue'

description: 'Removes all column labels from an issue'

inputs:
  token:
    description: 'The token'
    required: true
  issue_id:
    description: 'The issue id'
    required: true
  organization_name:
    description: 'The organization name'
    required: true
  repository_name:
    description: 'The repository name'
    required: true

runs:
  using: 'node12'
  main: 'dist/index.js'
